
syntax = "proto3";

package dnss;

message RawMsg {
	// DNS-encoded message.
	// A horrible hack, but will do for now.
	bytes data = 1;
}

service DNSService {
	rpc Query(RawMsg) returns (RawMsg);
}

